spring:
  cloud:
    config:
      uri: http://localhost:8888
      # name을 서비스명으로 맵핑할 경우 해당 config-service가 default로 호출하는 application.yml을 읽어옴
      name: config-service # ecommerce
  # 읽어올 profile을 지정
#  profiles:
#    active: dev